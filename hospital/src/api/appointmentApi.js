import axiosClient from './axiosClient';
export const appointmentApi = {
  // getAll:        ()       => axiosClient.get('/appointments'),
  // getByPatient:  (pid)    => axiosClient.get(`/appointments/patient/${pid}`),
  // getByDoctor:   (did)    => axiosClient.get(`/appointments/doctor/${did}`),
  // create:        (data)   => axiosClient.post('/appointments', data),
  // updateStatus:  (id, s)  => axiosClient.patch(`/appointments/${id}/status`, { status: s }),
  // cancel:        (id)     => axiosClient.delete(`/appointments/${id}`),
};
