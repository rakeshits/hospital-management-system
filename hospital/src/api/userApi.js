import axiosClient from './axiosClient';
export const userApi = {
  // getAll:   ()      => axiosClient.get('/users'),
  // update:   (id, d) => axiosClient.put(`/users/${id}`, d),
  // setActive:(id, v) => axiosClient.patch(`/users/${id}/active`, { active: v }),
};
