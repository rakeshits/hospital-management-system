import axiosClient from './axiosClient';
export const medicalRecordApi = {
  // getByPatient: (pid)   => axiosClient.get(`/records/patient/${pid}`),
  // create:       (data)  => axiosClient.post('/records', data),
};
